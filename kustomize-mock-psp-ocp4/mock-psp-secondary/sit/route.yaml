apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: "mock-psp-sec" 
spec:
  host: test.nexi.ndp.pagopa.it
  port:
    targetPort: 8091
  path: "/mock-psp-secondary-sit"
  to:
    kind: Service
    name: "psp-sec-mock"
    weight: 100
  wildcardPolicy: None

