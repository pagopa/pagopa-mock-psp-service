apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: "mock-psp" 
spec:
  host: test.nexi.ndp.pagopa.it
  port:
    targetPort: 8090
  path: "/mock-psp-primary-sit"
  to:
    kind: Service
    name: "mock-psp"
    weight: 100
  wildcardPolicy: None

